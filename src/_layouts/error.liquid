<!doctype html>
<html lang="en" class="layout-error">
  <head>
    {% render "head", metadata: site.metadata, title: resource.data.title %}

    <link rel="stylesheet" href="{% asset_path stylesheets/error.scss %}" type="text/css"/>
    {% live_reload_dev_js %}
  </head>
  <body class="{{ resource.data.page_class }}">
    {{ content }}
  </body>
</html>
